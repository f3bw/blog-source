import React from "react";
import "./App.css"; // Ensure this imports your CSS
import StaggerButton from "./StaggerButton"; // Import the StaggerButton component

const App = () => {
  return (
    <div>
      <StaggerButton /> {/* Use the StaggerButton component */}
    </div>
  );
};

export default App;
